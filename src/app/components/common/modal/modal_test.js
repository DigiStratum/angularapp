'use strict';

describe('Modal module', function() {

	beforeEach(module('Modal'));

	describe('Modal controller', function(){
		it('should ....', inject(function($controller) {
			//spec body
			var modalCtrl = $controller('ModalCtrl');
			expect(modalCtrl).toBeDefined();
		}));

	});
});

